<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>World Clock App</title>
    <!-- Include Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Include Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f2f5;
        }
        .world-clock-card {
            max-width: 400px;
            margin: 50px auto;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            background-color: #b6ced4;
            position: relative;
            /* To position the globe icon */
        }
        .world-clock-title {
            font-size: 24px;
            font-weight: bold;
            color: #333;
            margin-bottom: 20px;
        }
        .input-group-text {
            background-color: #fff;
            border: none;
        }
        .world-clock-icon {
            font-size: 48px;
            margin-bottom: 20px;
        }
        .world-clock-info {
            font-size: 18px;
            color: #555;
            margin-bottom: 10px;
        }
        .world-clock-btn {
            background-color: #007bff;
            border: none;
        }
        .world-clock-btn:hover {
            background-color: #0056b3;
        }
        .globe-icon {
            position: absolute;
            top: 10px;
            right: 10px;
            color: #ffce00;
            font-size: 48px;
        }
    </style>
</head>
<body onload="getAllCountries()">
    {{>header}}
    <div class="world-clock-card">
        <div class="row justify-content-end">
            <div class="col-auto">
                <div class="globe-icon">
                    <i class="bi bi-globe"></i>
                </div>
            </div>
        </div>
        <h1 class="world-clock-title text-center mb-4">World Clock App</h1>

        <div class="input-group mb-3">
            <select name="country" id="country" class="form-control" onchange="getallState(value)">
                <option value="">---Select country---</option>
                <!-- You can dynamically populate options here -->
            </select>
        </div>

        <div class="input-group mb-3">
            <select name="state" id="state" class="form-control" onchange="getallcity(value)">
                <option value="">---Select state---</option>
                <!-- You can dynamically populate options here -->
            </select>
        </div>

        <div class="input-group mb-3">
            <select name="city" id="city" class="form-control" onchange="worldclockdata(value)">
                <option value="">---Select city---</option>
                <!-- You can dynamically populate options here -->
            </select>
        </div>
        <div id="worldClockInfo">
            <div class="world-clock-icon text-center">
                <i class="bi bi-clock"></i>
            </div>
            <div class="world-clock-info text-center">
                <p>City : <span id="mycity">{{city}}</span></p>
                <p>Date : <span id="mydate">{{date}}</span></p>
                <p>Time : <span id="mytime">{{time_24}}</span></p>
                <p>Timezone : <span id="mytimezone">{{timezone}}</span></p>
            </div>
        </div>
    </div>
    <!-- Include Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="js/data.js"></script> <!-- Assuming this script contains the function getWorldClockData -->
</body>
</html>
